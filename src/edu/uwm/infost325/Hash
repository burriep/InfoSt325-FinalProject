//This class is meant to run in the background, accepting user input and
//returning a hashed version of what the user input based on the SHA2 algorithm.

import java.io.IOException;
inport java.io.OutputStream;
import java.security.ProviderException;
import java.util.concurrent.ConcurrentHashMap;


public class 
public static int hash1(int test){
			for (int i = 0; i <= test.length; i++){
      
      
			
		}
